# workout-block 改进清单

> 状态：草稿（待开发者逐项认领）
> 来源：2026-08-28 跑步记录字段扩容实操中发现的问题（字段 long/time → distance/duration_sec 改名 + 新增 avg_pace/calories）
> 定位：给插件的结构性改进建议，按影响面与收益排优先级。

## 优先级说明

- **P1**：本次实操中真实踩坑 / 会造成数据风险，建议优先做
- **P2**：体验与结构优化，收益明确但不阻塞
- **P3**：锦上添花

---

## P1 字段 key 改名会孤立历史数据（无迁移机制）

**问题**：`config update-type --fields-json` 是整体替换字段数组；字段 key 一旦改名，历史 CSV 记录里该字段的旧值就变成"无人认领"数据——列表不显示、统计不算入，且无任何报错提示。本次迁移 2 条跑步记录只能手工「按原值重录 → 软删旧行 → compact」，纯人工操作、易错。

**现状**：`src/data/configMigrate.ts` 只做结构补全（补缺失字段/单位迁移），没有字段级 key 改名能力；CSV 的 fields 是 JSON blob（`csvFormat.ts`），字段与记录之间靠 key 字符串耦合。

**建议**（三选一，按推荐度排序）：
1. **FieldDef 增加 `legacyKeys?: string[]`**：update-type 改 key 时，读取器按 legacyKeys 把旧记录值映射到新 key（`display.ts`/`statExpr.ts` 取值处加一层别名解析）。改动最小、零数据迁移、向后兼容。
2. **提供 `config rename-field --type X --field old --new-key Y` 命令**：自动改写该类型全部记录的 fields JSON + 更新类型定义 + 校验统计公式引用，一次原子完成。改动最大但最彻底。
3. **至少给 update-type 增加破坏性提示**：检测到字段 key 变更时，输出受影响记录数与统计项清单，要求 `--yes` 确认，防止静默丢数据。

## P1 统计公式引用字段 key，改名时无静态检查

**问题**：统计项公式（`sum:distance`、`sum(reps*weight)` 等）直接引用字段 key；字段改名后公式静默失效（sum 空值），`config stats` 不报错。本次排查统计项靠人工 `config stats` 逐条核对。

**建议**：`config update-type`（及字段增删）时对全部启用统计做公式字段引用校验，输出「受影响统计项清单」；或在 `doctor` 体检中新增一项「统计公式引用失效检测」（只读、可自愈报告）。

## P1 workout-block-record/SKILL.md 缺字段改名的风险警告

**问题**：技能文档没有记录「字段改名会孤立历史数据 + 迁移三步法（重录→软删→compact）」，下次任何 agent 改字段 key 都会再踩。

**建议**：在 SKILL.md 的「写入配置」小节补一条警告块：字段 key 改名不迁移旧值；必须迁移时按三步法，且先跑 `config stats` 排查统计公式引用。具体文案可与《README》安全设计节合并。

---

## P2 无派生/计算字段，「配速」这类派生量只能落库

**问题**：`FieldDef.inputType` 只有 `number | duration | text | select`（`src/data/types.ts`），没有计算字段。平均配速 = 时长÷距离，属于派生量，却只能作为独立字段让用户手填，存在「距离/时长/配速三者不一致」的数据污染风险。

**建议**：为字段增加 `computed` 语义（如 `FieldDef.formula?: string`，沿用 `statExpr.ts` 的表达式求值器）：计算字段不落库，渲染与统计时动态求值。跑步配速、配速下的速度（km/h）都能顺带覆盖。

## P2 duration 字段渲染不拼接单位（配速显示缺「/公里」）

**问题**：`display.ts renderFieldValue` 对 duration 只做 `formatDuration(秒)`，不追加 `unitLabel`；本次新增的 `avg_pace` 字段（unitLabel="/公里"）在列表里只会显示「5分46秒」，没有单位后缀，容易和训练时长混淆。

**建议**：渲染 duration 字段时若配置了 `unitLabel` 则追加（「5分46秒/公里」）；或在 RecordModal/列表 UI 层为 duration 字段统一补单位。需要同步核对 RecordModal、日志代码块渲染、CLI list 输出三处。

## P2 训练类型 id 用中文作标识符

**问题**：`距离类训练` 的类型 id 是中文（`workout-config.json` 中 `"id": "距离类训练"`），而训练项 id 是英文 slug（`running`）+ `nameKey` 走 i18n（`display.ts DEFAULT_TRAINING_TYPE_NAME_KEYS` 只有 strength/aerobic/bodyweight 三个内置）。中文 id 在跨语言、路径、命令行传参时都容易出问题（本次 CLI 传参虽可用，但属于侥幸）。

**建议**：类型 id 规范化为英文 slug（`distance`/`cardio` 等），中文名走 `name`/`nameKey`；提供 `config update-type --new-id` 级联改写（已有此能力），并补一条迁移说明。

## P3 建议：配速专用输入类型

**问题**：duration 型配速字段语义模糊（录的是「每公里秒数」，显示却走通用时长格式化），统计聚合时长对配速意义有限。

**建议**：若做了 P2 计算字段，配速直接用 computed 派生，无需专用类型；若不做，可考虑 `inputType: 'pace'` 专用类型（格式化「X分Y秒/公里」、支持 `5:46` 输入）。

---

## 附录：本次字段改名的参考流程（供 SKILL 文档补写）

1. 备份：`workout-config.json` + `workout_logs.csv` → 项目 `.workbuddy/backup/YYYY-MM-DD/`（放 vault 外，避免污染知识库）；
2. `config stats` 排查统计公式是否引用被改字段；
3. `config update-type --fields-json ...` 整体替换字段数组；
4. 逐条按原值重录（`add` + 原 `--time`/`--note`）→ `delete --id <旧id>` → `compact`；
5. 验证：`resolve 跑步`、`list`、`doctor`（0 墓碑 / 0 脏行 / 存活数不变）。

涉及文件：`src/data/types.ts`、`src/data/configMigrate.ts`、`src/data/csvFormat.ts`、`src/data/display.ts`、`src/data/statExpr.ts`、`src/cli/commands/configWriteCmd.ts`、`workout-block-record/SKILL.md`
